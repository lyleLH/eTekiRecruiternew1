//
//  SuggestSkillViewModalTests.swift
//  eTekiRecruiterTests
//
//  Created by Afreen Shaik on 03/10/19.
//  Copyright © 2019 amzurtech. All rights reserved.
//

import XCTest
@testable import eTekiRecruiter
class SuggestSkillViewModalTests: XCTestCase {
//   var suggestSkillModal = SuggestSkillViewModel()
    
//    func testgetSkillsAutocomplete(){
//      let manager = CreateJobManager(skill: "Swift", skillType: "required")
//        let getAutoCompleteResponse = CreateJobResponse(json: ["autoSkills": ["Swift"]], success: true, message: "skill added")
//        CreateJobStubApi.skillsAutoComplete = getAutoCompleteResponse
//        suggestSkillModal.createJobAPI = CreateJobStubApi.self
//        suggestSkillModal.getSkillsAutoCompleted(createJobManager:manager , requestType: ServerAttributes.skillsAutoComplete)
//    }
//    
//    func testsuggestSkill(){
//        let manager = CreateJobManager(skill: "Swift", skillType: "required")
//        let suggestSkillResponse = CreateJobResponse(json: ["autoSkills": ["Swift"]], success: true, message: "skill added")
//        CreateJobStubApi.suggestSkill = suggestSkillResponse
//        suggestSkillModal.createJobAPI = CreateJobStubApi.self
//       suggestSkillModal.suggestSkill(createJobManager: manager, requestType: ServerAttributes.suggestSkills)
//    }
    
    override func setUp() {
        // Put setup code here. This method is called before the invocation of each test method in the class.
    }

    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
    }

    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
    }

    func testPerformanceExample() {
        // This is an example of a performance test case.
        self.measure {
            // Put the code you want to measure the time of here.
        }
    }

}
